---
name: <kebab-case-name>
description: >
  Apply when working on files under <path/glob> — <what the skill governs>. Contains the
  mandatory pipeline, the rules, and the anti-patterns. Do not trigger for <when it should
  NOT load>.
---

# <Human title>

## Mandatory pipeline

<If this skill enforces an order, state it first and make it non-negotiable.>

```
<step> → <step> → <step>
```

## Rules

- <Rule. Reference tasks (`just test`), never literal stack commands.>
- <Rule.>

## Anti-patterns (never)

- <Explicit "never do X" — agents follow these better than prose.>
- <…>

## Examples

<Short, concrete. A snippet that shows the right shape.>

## References

<Link to references/ files for longer checklists or templates — keep this body loadable.>
